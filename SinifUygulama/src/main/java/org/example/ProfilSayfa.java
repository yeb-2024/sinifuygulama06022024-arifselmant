package org.example;

import javax.swing.*;

public class ProfilSayfa {
    JFrame frame = new JFrame();
    JLabel isimLabel = new JLabel("İsim :Arif");

    ProfilSayfa(){
        frame .setSize(500,500);
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        frame.setLayout(null );
        frame.setVisible(true);


        }
}
